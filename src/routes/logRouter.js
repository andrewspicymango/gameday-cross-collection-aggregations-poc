const express = require('express');
const { _setDebugLogLevel, _setInfoLogLevel } = require('../controllers/logController');
const router = express.Router();

////////////////////////////////////////////////////////////////////////////////
router.post('/debug', _setDebugLogLevel);

////////////////////////////////////////////////////////////////////////////////
router.post('/info', _setInfoLogLevel);

////////////////////////////////////////////////////////////////////////////////
module.exports = router;
